# Wan 2.2 T2V 파이프라인 진행 기록 (2025-02-25 최종 업데이트)

## 현재 상태: VAE decoder 색상 왜곡 수정 필요 🔴

**확정된 사실**:
- ✅ **Transformer 정상 확인됨** — MAX transformer latent → diffusers VAE 디코딩 시 정상 출력
- ✅ **Scheduler 정상 확인됨** — UniPC 격리 테스트 max_diff < 0.003, cos ≈ 1.0
- 🔴 **VAE decoder만 문제** — 동일 latent에서 MAX VAE가 색상 왜곡 (green/yellow tint, blue 억제)

---

## 1. 유저 요청 (원문)
1. "@진행상황_기록.md 을 보고 wan t2v 끝까지 완료해줘"
2. "conda test 환경에 diffusers가 설치되어 있으니까 사용하고 origin/add/wan-pipeline/full-pipeline에서는 transformers 잘 됐었어"
3. Target command:
```bash
./bazelw run //max/examples/diffusion:simple_offline_generation -- \
  --model Wan-AI/Wan2.2-T2V-A14B-Diffusers \
  --prompt "Two anthropomorphic cats in comfy hoodies sitting at a wooden desk" \
  --num-inference-steps 40 --guidance-scale 4.0 --guidance-scale-2 3.0 \
  --output t2v_output_720p.mp4 --num-frames 81 --height 720 --width 1280
```
4. "속도와 메모리 측면에서 diffusers와 유사하거나 더 좋은 성능을 얻는 걸 필요로 한다"
5. "diffusers vae 사용하면 안 돼. 목표는 max native야"
6. "자 목표는 max vae의 성능을 cuDNN을 쓰는 등 해서라도 diffusers 성능만큼 끌어올리는 거야"
7. "일단 목표는 정확도야!" (accuracy first, speed second)
8. "high_noise_model low_noise_model 이런식으로 써줘" (naming convention)
9. "만약 안되겠으면 max native로 구현하고, tile 해서라도 e2e 완료해줘"

## 2. 최종 목표
Wan 2.2 T2V pipeline **end-to-end at 480p and 720p** (81 frames), producing valid `.mp4` video.
- **Transformer**: ✅ 정상 (diffusers VAE로 검증 완료)
- **Scheduler**: ✅ UniPC multistep (검증 완료)
- **VAE decoder**: 🔴 MAX native 구현 필요 (색상 왜곡 수정)
- **Performance**: VRAM/속도 diffusers 대비 과도한 차이 없을 것
- **완료 후**: e2e 테스트 스크립트 제공 → 유저가 마무리

## 3. 환경 정보
- conda `test` env: `/root/miniconda3/envs/test/bin/python3` (diffusers 0.36.0, torch 2.10.0+cu128)
- Build: `./bazelw run -c opt` (ptxas already in PATH, MODULAR_NVPTX_COMPILER_PATH 불필요)
- GPU: NVIDIA H200, 143GB VRAM
- Branch: `add/wan-pipeline/full-pipeline-tmp`
- Output dir: `/tmp/wan_t2v_outputs/`

---

## 4. 완료된 작업 ✅

### 커널 레벨
1. cuDNN 3D Conv struct alignment 버그 수정
2. cuDNN INT32 stride overflow 수정
3. Algorithm caching for conv3d

### Transformer
4. Denoising 정확성 검증 완료
5. MoE dual-transformer 지원 (boundary_ratio=0.875)
6. Naming cleanup: `_cfg_model` → `_guidance_model`, `high_noise_model`/`low_noise_model`
7. Float32 latent loop (bf16은 transformer input만)
8. **Transformer 퀄리티 최종 확인**: MAX transformer latent → diffusers VAE = 정상 출력 ✅

### Scheduler
9. UniPC multistep scheduler 구현 (611줄, 5 tests passing, max_diff ~4e-6)
10. Pipeline 통합 완료 (CPU float64 precision)

### VAE Decoder
11. Monolithic 2-graph decoder (`_WanVAEDecoderFirstFrame` + `WanVAEDecoder`)
12. `denormalize_vae_latents` 수식 수정: `/ recip_std` → `* recip_std`
13. rest_chunk=1 → rest_chunk=5로 변경 (temporal context 개선)
14. RMSNorm gamma 검증 (correct)
15. Weight loading 검증 (Conv2d transpose, Conv3d no-transpose)
16. Temporal interleave 검증 (correct)
17. Upsample2d 검증 (correct)

### Pipeline Infrastructure
18. `--guidance-scale-2` CLI argument
19. ffmpeg video saving (RGBA→RGB, deadlock fix)
20. `run_wan_t2v.sh` e2e test script
21. Transformer offloading before VAE decode

---

## 5. VAE 문제 상세 분석 🔴

### 근본 원인 (확정)
**Temporal context 손실**: `rest_chunk=1`일 때 각 프레임이 독립 디코딩되어 causal Conv3d에 zero padding만 들어감. diffusers는 feat_cache로 이전 프레임 activations를 전달.

### rest_chunk=5 적용 결과
- R/G 채널 크게 개선 (diff 0.13→0.01)
- B 채널 여전히 ~0.07 부족 (mp4 YUV 인코딩 손실 가능성 + VAE 버그)
- **chunk 경계마다 flickering** (매 20 video frames)

### rest_chunk=20 시도 → OOM
- 480p에서도 CUDA_ERROR_OUT_OF_MEMORY
- cuDNN 3D conv workspace allocation failure → VRAM 누적

### 검증한 것들 (VAE 버그 아닌 것)
| Component | Status | Notes |
|---|---|---|
| RMSNorm gamma shape | ✅ | [dim,1,1,1] for 5D, images=False → (1,1,1) trailing |
| RMSNorm math | ✅ | x * rsqrt(mean(x²)) * γ ≡ diffusers F.normalize |
| Temporal interleave | ✅ | reshape+permute+reshape ≡ stack |
| Upsample2d | ✅ | concat-based nearest 2x ≡ nn.Upsample nearest-exact |
| Conv2d weights | ✅ | FCRS→RSCF transpose correct |
| Conv3d weights | ✅ | FCQRS kept as-is (permute=True) |
| Causal padding | ✅ | (2*pad_t, 0, pad_h, pad_h, pad_w, pad_w) correct |
| Scheduler | ✅ | Isolation test: max_diff < 0.003 |
| Transformer | ✅ | diffusers VAE decode = good quality |

### 아직 검증 안 된 것
| # | 의심 항목 | 설명 |
|---|---|---|
| 1 | **Conv3d forward() permute=True** | cuDNN dispatch 시 NDHWC↔NCDHW permute + FCRS filter layout. 내부 연산 정확성 미검증 |
| 2 | **Batch temporal decode vs feat_cache** | causal conv에서 batch 처리가 frame-by-frame feat_cache와 수학적으로 동등한지 |
| 3 | **first_decoder frame 0 정확도** | frame 0도 diffusers 대비 off (R 30% 차이). mp4 손실인지 real bug인지 미확인 |

---

## 6. 이어서 할 작업 (우선순위 순)

### Step 1: VAE raw tensor 비교 (mp4 손실 제거)
MAX VAE raw output과 diffusers VAE raw output을 numpy로 직접 비교. 이미 저장 로직 추가됨.

```bash
# MAX pipeline 실행 (latent + raw VAE output 저장됨)
WAN_LOAD_LATENT=/tmp/wan_t2v_outputs/diffusers_final_latent.npy \
  ./bazelw run -c opt //max/examples/diffusion:simple_offline_generation -- \
  --model Wan-AI/Wan2.2-T2V-A14B-Diffusers \
  --prompt "test" --num-inference-steps 40 --guidance-scale 4.0 --guidance-scale-2 3.0 \
  --output /tmp/wan_t2v_outputs/test.mp4 --num-frames 81 --height 480 --width 832 --seed 42

# 저장 위치:
# - /tmp/wan_t2v_outputs/max_final_latent.npy (denorm 전 latent)
# - /tmp/wan_t2v_outputs/max_vae_raw_output.npy (VAE decoder raw output, [-1,1], BCTHW)
```

### Step 2: Layer-by-layer 비교
first_decoder (frame 0, T=1) 에서 각 layer 출력을 diffusers와 비교해서 어디서 diverge 하는지 찾기.

### Step 3: 수정 적용 & 검증
버그 수정 후:
```bash
# VAE-only 테스트 (동일 latent 비교)
WAN_LOAD_LATENT=/tmp/wan_t2v_outputs/diffusers_final_latent.npy \
  ./bazelw run -c opt //max/examples/diffusion:simple_offline_generation -- \
  --model Wan-AI/Wan2.2-T2V-A14B-Diffusers \
  --prompt "test" --num-inference-steps 40 --guidance-scale 4.0 --guidance-scale-2 3.0 \
  --output /tmp/wan_t2v_outputs/vae_fixed.mp4 --num-frames 81 --height 480 --width 832 --seed 42
```

### Step 4: Full E2E 480p
```bash
./bazelw run -c opt //max/examples/diffusion:simple_offline_generation -- \
  --model Wan-AI/Wan2.2-T2V-A14B-Diffusers \
  --prompt "Two anthropomorphic cats in comfy hoodies sitting at a wooden desk" \
  --num-inference-steps 40 --guidance-scale 4.0 --guidance-scale-2 3.0 \
  --output /tmp/wan_t2v_outputs/e2e_480p.mp4 --num-frames 81 --height 480 --width 832 --seed 42
```

### Step 5: Full E2E 720p
```bash
./bazelw run -c opt //max/examples/diffusion:simple_offline_generation -- \
  --model Wan-AI/Wan2.2-T2V-A14B-Diffusers \
  --prompt "Two anthropomorphic cats in comfy hoodies sitting at a wooden desk" \
  --num-inference-steps 40 --guidance-scale 4.0 --guidance-scale-2 3.0 \
  --output /tmp/wan_t2v_outputs/e2e_720p.mp4 --num-frames 81 --height 720 --width 1280 --seed 42
```

### Step 6: Cleanup
- Debug code 제거 (WAN_LOAD_LATENT hack, latent/raw save)
- E2E test script 최종화
- 진행상황 기록 최종 업데이트

---

## 7. Diffusers reference 파일들
| 파일 | 내용 |
|---|---|
| `/tmp/wan_t2v_outputs/diffusers_final_latent.npy` | diffusers transformer 최종 latent (1,16,21,60,104) |
| `/tmp/diffusers_reference_frames.npy` | diffusers 전체 81 frames (81,480,832,3) float32 [0,1] |
| `/tmp/diffusers_all_step_latents.npy` | 40 steps 전체 latent (40,1,16,21,60,104) |
| `/tmp/wan_t2v_outputs/max_transformer_diffusers_vae.mp4` | **MAX transformer + diffusers VAE** = 정상 품질 ✅ |
| `/tmp/wan_t2v_outputs/e2e_480p_chunk5.mp4` | MAX transformer + MAX VAE (chunk=5) = 품질 이상 🔴 |

## 8. MAX VAE 디코딩 스크립트 (diffusers VAE로 확인용)
```bash
# MAX transformer latent을 diffusers VAE로 디코딩 (transformer 품질 확인)
/root/miniconda3/envs/test/bin/python3 /tmp/decode_with_diffusers_vae.py \
  /tmp/wan_t2v_outputs/max_final_latent.npy \
  /tmp/wan_t2v_outputs/max_transformer_diffusers_vae.mp4
```

---

## 9. 핵심 파일
| 파일 | 설명 | 상태 |
|---|---|---|
| `max/python/.../wan/pipeline_wan.py` | 메인 파이프라인 (~667줄) | **수정됨** (debug code 있음) |
| `max/python/.../autoencoders/autoencoder_kl_wan.py` | VAE decoder (~895줄) | **수정됨** (🔴 버그 있음) |
| `max/python/.../autoencoders/model_config.py` | VAE config | 변경없음 |
| `max/python/.../diffusion_schedulers/scheduling_unipc_multistep.py` | UniPC scheduler | ✅ 완료 |
| `max/python/max/nn/conv.py` | Conv2d/Conv3d (permute=True/False) | 미검증 (Conv3d forward) |
| `max/examples/diffusion/simple_offline_generation.py` | CLI entry point | **수정됨** |
| `max/examples/diffusion/run_wan_t2v.sh` | E2E test script | **수정됨** |
| diffusers VAE 참조: `/root/miniconda3/envs/test/lib/python3.13/site-packages/diffusers/models/autoencoders/autoencoder_kl_wan.py` | |

## 10. Scheduler Config
```json
{
  "num_train_timesteps": 1000,
  "prediction_type": "flow_prediction",
  "predict_x0": true,
  "solver_order": 2,
  "solver_type": "bh2",
  "lower_order_final": true,
  "use_flow_sigmas": true,
  "flow_shift": 3.0,
  "time_shift_type": "exponential",
  "final_sigmas_type": "zero"
}
```

## 11. 명시적 제약 (유저 원문)
- "속도와 메모리 측면에서 diffusers와 유사하거나 더 좋은 성능을 얻는 걸 필요로 한다"
- "diffusers vae 사용하면 안 돼. 목표는 max native야"
- "자 목표는 max vae의 성능을 cuDNN을 쓰는 등 해서라도 diffusers 성능만큼 끌어올리는 거야"
- "일단 목표는 정확도야!" (accuracy first)
- "만약 안되겠으면 max native로 구현하고, tile 해서라도 e2e 완료해줘"
